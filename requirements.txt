# Paddy 量化工作台依赖
# 数据 / 回测 / 信号核心
yfinance>=0.2.40
pandas>=2.0.0
numpy>=1.24.0
# Web 面板
streamlit>=1.30.0
plotly>=5.0.0
# 网络请求 (加密行情)
requests>=2.31.0
# 加密实时行情 WebSocket
websocket-client>=1.7.0
# 面板自动刷新 (实时模式 / 实时行情)
streamlit-autorefresh>=0.0.3
# 配置
pyyaml>=6.0.1
# A股数据 (akshare, 前复权历史K线；provider 已做 lazy import，缺失也不影响美股/加密)
akshare>=1.12.0
# 本地落库 / 缓存 (SQLite 用 stdlib；parquet 缓存需要 pyarrow)
pyarrow>=14.0.0
# 如用 Postgres 落库再装 sqlalchemy
